# A basic implementation of arbitrage


## Building
```bash
  git clone https://github.com/jakehf90s/CryptoCPP.git
  cd CryptoCPP
  mkdir build
  cd build
  cmake -DCMAKE_PREFIX_PATH=QT_PATH ..
  make
